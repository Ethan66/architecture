/* export const calculate = (...args) => {
    return args.reduce((sum, cur) => {
        return sum + cur
    }, 0)
} */

export const calculate = (a) => {
    if (a> 1) {
        return 1
    }
    return 3
}